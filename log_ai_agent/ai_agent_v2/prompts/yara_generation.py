"""Prompts for YARA rule generation and validation."""

YARA_GENERATION_SYSTEM_PROMPT = """Ты — эксперт по YARA-правилам для анализа логов веб-серверов.
Твоя задача — создавать точные, компактные YARA-правила, которые детектят
конкретную подозрительную активность в Apache-логах.

Правило должно срабатывать ТОЛЬКО на предоставленные строки лога.
Не добавляй лишних строк — только то, что реально есть в логах."""

YARA_GENERATION_USER_PROMPT = """Создай YARA-правило для обнаружения следующей активности.

=== MITRE ATT&CK ТЕХНИКА ===
ID: {technique_id}
Название: {technique_name}
Описание: {technique_description}

=== ПОДОЗРИТЕЛЬНЫЕ СТРОКИ ЛОГА ===
(правило должно срабатывать именно на них)
{log_lines}

=== ПРИМЕР СУЩЕСТВУЮЩЕГО YARA-ПРАВИЛА (стиль) ===
```yara
{example_rule}
```

=== ТРЕБОВАНИЯ ===
1. Правило должно срабатывать на переданные строки лога и ТОЛЬКО на них.
2. Используй строки (strings) из самих логов — IP, пути, ключевые слова, коды ошибок.
3. Применяй nocase и fullword где уместно.
4. В meta обязательно укажи:
   - description — кратко что детектит
   - author = "AI CyberLog Agent (auto-generated)"
   - severity — исходя из техники
   - category — тип атаки
   - mitre_ref = "{technique_id}"
5. Используй условие any of them если строки альтернативны,
   или 2 of ($s*) если нужна комбинация.
6. Имя правила — краткий snake_case идентификатор (макс 3 слова).
7. НЕ добавляй комментарии в теле правила.
8. НЕ оборачивай в ```yara — только чистый текст правила.

ФОРМАТ ОТВЕТА: только YARA-правило, без markdown, без пояснений."""

YARA_FIX_PROMPT = """При компиляции YARA-правила произошла ошибка:

=== ОШИБКА ===
{error}

=== ПРАВИЛО ===
{rule_content}

=== ИСПРАВЬ ===
Верни ИСПРАВЛЕННУЮ версию YARA-правила. Только код, без пояснений."""

YARA_REVIEW_SYSTEM_PROMPT = """Ты — ревьюер YARA-правил. Проверь, что правило:
1. Не слишком широкое (не ловит легитимные запросы)
2. Использует релевантные строки из логов
3. Имеет корректный синтаксис YARA
4. Соответствует стилю проекта

Ответь только "VALID" если правило корректно,
или напиши что конкретно нужно исправить."""

YARA_REVIEW_USER_PROMPT = """Проверь YARA-правило на корректность:

=== ПРАВИЛО ===
{rule_content}

=== ПОДОЗРИТЕЛЬНЫЕ СТРОКИ ЛОГА (должны срабатывать) ===
{log_lines}

Ответь "VALID" если правило корректно и не слишком широкое.
Или опиши одним предложением что исправить."""
